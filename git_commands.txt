git commands

git config user.name H-Systems
git config user.email h-systems@naver.com

git init
git status
git remote add origin https://github.com/H-Systems/TestGit2.git
(Add remote repository)

git status
git add <file name> or <everyting files>

git commit -m 'bla bla'
git push -u origin master
(commit)

- modified file
git add
git commited -m
git push

Local File을 수정했습니다.
